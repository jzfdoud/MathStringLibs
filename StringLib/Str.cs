using Microsoft.VisualBasic;
using System;
using System.Security.Cryptography.X509Certificates;

namespace StringLib
{
    public class Str
    {
        public string Reverse(string a)
        {
            //think of an array. Define the string, define the length of the string. 
            //Begin for loop to run through the length of the string and go around until the string is reversed

            return
        }

        public string Rotate(string a) 
        {
            //dictionary, add( current char, next char). foreach loop 
        }

        public struct RemoveVowels
        {
            public RemoveVowels()
                {
               
            }
        }


    }
}
